{
  "name": "@dotnaos/skillpack",
  "version": "0.1.0",
  "description": "Install multiple skills from multiple repositories in one command.",
  "type": "module",
  "bin": {
    "skillpack": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2"
  },
  "packageManager": "bun@1.2.14"
}
